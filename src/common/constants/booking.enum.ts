export enum BookingStatus {
    WAITING = 'WAITING',
    CONFIRMED = 'CONFIRMED',
    DONE = 'DONE',
    CANCEL = 'CANCEL',
    EXPIRED = 'EXPIRED',
}

export enum BookingType {
    FOR_MYSELF = 'FOR_MYSELF',
    FOR_RELATIVES = 'FOR_RELATIVES',
}